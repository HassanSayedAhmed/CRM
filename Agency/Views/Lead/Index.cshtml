@using CRM.Enums;

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="modal modal animated slideInUp custo-slideInUp" id="manage_lead_details_modal" tabindex="-1" role="dialog" aria-labelledby="tabsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="tabsModalLabel">Lead Details</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <ul class="nav nav-tabs mb-3" id="myTab" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" id="leaddetails-tab" data-toggle="tab" href="#leaddetails" role="tab" aria-controls="LeadDetails" aria-selected="true">Lead Details</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="requirements-tab" data-toggle="tab" href="#requirements" role="tab" aria-controls="Requirements" aria-selected="false">Requirements</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="interestedprojects-tab" data-toggle="tab" href="#interestedprojects" role="tab" aria-controls="InterestedProjects" aria-selected="false">Interested Projects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="employementdetails-tab" data-toggle="tab" href="#employementdetails" role="tab" aria-controls="EmployementDetails" aria-selected="false">Employement Details</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="location-tab" data-toggle="tab" href="#location" role="tab" aria-controls="Location" aria-selected="false">Location</a>
                    </li>
                </ul>
                <div class="tab-content" id="myTabContent">
                    <div class="tab-pane fade show active" id="leaddetails" role="tabpanel" aria-labelledby="leaddetails-tab">
                        <table class="table table-striped">
                            <tr>
                                <td style="font-weight:bold;">First Name</td>
                                <td><span id="lead_details_first_name"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Last Name</td>
                                <td><span id="lead_details_last_name"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Email</td>
                                <td><span id="lead_details_email"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Primary Phone</td>
                                <td><span id="lead_details_primary_phone"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Secondary Phone</td>
                                <td><span id="lead_details_secondary_phone"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Alternative Number</td>
                                <td><span id="lead_details_alternative_number"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Date of Birth</td>
                                <td><span id="lead_details_date_of_birth"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Date of Anniversary</td>
                                <td><span id="lead_details_date_of_anniversary"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Source</td>
                                <td><span id="lead_details_source"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Sales Agent</td>
                                <td><span id="lead_details_sales_agent"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Address</td>
                                <td><span id="lead_details_address"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Country</td>
                                <td><span id="lead_details_country"></span></td>
                            </tr>
                        </table>
                    </div>
                    <div class="tab-pane fade" id="requirements" role="tabpanel" aria-labelledby="requirements-tab">
                        <table class="table table-striped">
                            <tr>
                                <td style="font-weight:bold;">Property Type</td>
                                <td><span id="lead_details_property_type"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Sub Type</td>
                                <td><span id="lead_details_sub_type"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Unit Type</td>
                                <td><span id="lead_details_unit_type"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Requirement</td>
                                <td><span id="lead_details_requirement"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Budget Min</td>
                                <td><span id="lead_details_budget_min"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Budget Max</td>
                                <td><span id="lead_details_budget_max"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Minimum Area In</td>
                                <td><span id="lead_details_min_area_in"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Minimum Area In</td>
                                <td><span id="lead_details_max_area_in"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Area Metric</td>
                                <td><span id="lead_details_area_metric"></span></td>
                            </tr>
                            <tr>
                                <td style="font-weight:bold;">Remark</td>
                                <td><span id="lead_details_remark"></span></td>
                            </tr>
                        </table>
                    </div>
                    <div class="tab-pane fade" id="interestedprojects" role="tabpanel" aria-labelledby="interestedprojects-tab">

                    </div>
                    <div class="tab-pane fade" id="employementdetails" role="tabpanel" aria-labelledby="employementdetails-tab">

                    </div>
                    <div class="tab-pane fade" id="location" role="tabpanel" aria-labelledby="location-tab">

                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn" data-dismiss="modal"><i class="flaticon-cancel-12"></i> Discard</button>
                <button type="button" class="btn btn-primary">Save</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="manage_show_lead_activites_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <table class="table table-striped" id="show_activities_table">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                                <th>Duration</th>
                                <th>Note</th>
                                <th>Date</th>
                            </tr>
                            <tbody id="show_activities_table_body">

                            </tbody>
                        </table>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </form>
</div>


<div class="modal fade" id="manage_show_interests_modal" tabindex="-1" role="dialog" aria-labelledby="tabsModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="tabsModalLabel">Interests</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <ul class="nav nav-tabs mb-3" id="myTab" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" id="projects-tab" data-toggle="tab" href="#projects" role="tab" aria-controls="Projects" aria-selected="true">Projects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="properties-tab" data-toggle="tab" href="#properties" role="tab" aria-controls="Properties" aria-selected="false">Properties</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="developers-tab" data-toggle="tab" href="#developers" role="tab" aria-controls="Developers" aria-selected="false">Developers</a>
                    </li>
                </ul>
                <div class="tab-content" id="myTabContent">
                    <div class="tab-pane fade show active" id="projects" role="tabpanel" aria-labelledby="projects-tab">
                        <table class="table table-striped" id="projects_table">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                            </tr>
                            <tbody id="projects_table_body">
                            </tbody>
                        </table>
                    </div>
                    <div class="tab-pane fade" id="properties" role="tabpanel" aria-labelledby="properties-tab">
                        <table class="table table-striped" id="properties_table">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                            </tr>
                            <tbody id="properties_table_body">
                            </tbody>
                        </table>
                    </div>
                    <div class="tab-pane fade" id="developers" role="tabpanel" aria-labelledby="developers-tab">
                        <table class="table table-striped" id="developers_table">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                            </tr>
                            <tbody id="developers_table_body">
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn" data-dismiss="modal"><i class="flaticon-cancel-12"></i> Discard</button>
                <button type="button" class="btn btn-primary">Save</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="manage_quick_lead_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />
                            <div class="form-group col-md-6">
                                <label>First Name</label>
                                <input type="text" id="first_name" name="first_name" class="form-control" required />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Last Name</label>
                                <input type="text" id="last_name" name="last_name" class="form-control" required />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Phone</label>
                                <input type="number" id="phone1" name="phone1" class="form-control" required />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Property</label>
                                @Html.DropDownList("property_ids", new SelectList(ViewBag.Properties, "id", "name"), "Select Property", new { @name = "property_ids", @id = "property_ids", @multiple = "multiple" })
                            </div>
                            <div class="form-group col-md-6">
                                <label>Source</label>
                                @Html.DropDownList("source_id", new SelectList(ViewBag.Sources, "id", "name"), "Select Source", new { @class = "form-control", @name = "source_id", @id = "source_id" })
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="modal fade" id="manage_lead_stage_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />
                            <div class="form-group col-md-12">
                                <label class="col-form-label">Lead Stage</label>
                                <select class="form-control" id="lead_stage_id" name="lead_stage_id">
                                    <option value="2">FollowUp</option>
                                    <option value="3">Negotiation</option>
                                    <option value="4">Meeting</option>
                                    <option value="5">Deal</option>
                                </select>
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>


<div class="modal fade" id="manage_lead_deal_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />

                            @*<div class="form-group col-md-6">
                                    <label>Property</label>
                                    @Html.DropDownList("deal_property_id", new SelectList(ViewBag.Properties, "id", "name"), "Select Property", new { @class = "form-control", @name = "deal_property_id", @id = "deal_property_id", @required = "required" })
                                </div>
                            *@
                            <div class="form-group col-md-12">
                                <label>Price</label>
                                <input type="number" id="deal_property_price" name="deal_property_price" class="form-control" />
                            </div>
                            <div class="form-group col-md-12">
                                <label>User</label>
                                @Html.DropDownList("deal_make_user_id", new SelectList(ViewBag.DealUsers, "id", "full_name"), "Select User", new { @class = "form-control", @name = "deal_make_user_id", @id = "deal_make_user_id", @required = "required" })
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="modal fade" id="manage_lead_decision_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />

                            <div class="form-group col-md-12">
                                <label>Decision</label>
                                <select name="decision_id" id="decision_id" class="form-control">
                                    <option value="">Select Decision</option>
                                    <option value="1">Qualified</option>
                                    <option value="2">Not Qualified</option>
                                </select>
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="modal fade" id="manage_make_activity_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />
                            <input type="hidden" name="lead_id" id="lead_id" value="0" />

                            <div class="form-group col-md-6">
                                <label>Activity</label>
                                @Html.DropDownList("activity_id", new SelectList(ViewBag.Acitivities, "id", "name"), "Select Activity", new { @class = "form-control", @name = "activity_id", @id = "activity_id", @required = "required" })
                            </div>

                            <div class="form-group col-md-6">
                                <label>Activity Datetime</label>
                                <input type="date" name="activity_date_time" id="activity_date_time" class="form-control" />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Activity Duration(Min)</label>
                                <input type="number" name="activity_duration" id="activity_duration" class="form-control" />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Note</label>
                                <input type="text" name="note" id="note" class="form-control" />
                            </div>
                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="modal fade" id="manage_assign_Lead_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="lead_ids" id="lead_ids" />

                            <div class="form-group col-md-12">
                                <label>User</label>
                                @Html.DropDownList("assigned_user_id", new SelectList(ViewBag.Users, "id", "full_name"), "Select User", new { @class = "form-control", @name = "assigned_user_id", @id = "assigned_user_id", @required = "required" })
                            </div>

                        </div>

                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<div class="page-header">
    <nav class="breadcrumb-one" aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0);">Lead</a></li>
        </ol>
    </nav> <!-- we commented old button and add new button with the id of add resrvation in old dropdown menu-->
    <div class="btn-group">
        <button type="button" class="btn btn-primary">More</button>
        <button type="button" class="btn btn-primary dropdown-toggle dropdown-toggle-split" data-toggle="dropdown">
            <span class="caret"><i class="fa fa-arrow-alt-circle-down"></i></span>
        </button>
        <div class="dropdown-menu">
            <a class="dropdown-item" id="add_Lead_" href="@Url.Action("manageLead","Lead")">Add Full Lead</a>
            <a class="dropdown-item" id="add_quick_lead" href="javascript:void(0)">Add Quick Lead</a>
        </div>
    </div>
    <!--
        <div class="dropdown filter custom-dropdown-icon">
        <a class="dropdown-toggle btn" href="#" role="button" id="filterDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="text">More</span> <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-chevron-down"><polyline points="6 9 12 15 18 9"></polyline></svg></a>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="filterDropdown">
            <a class="dropdown-item" id="add_user" data-value="<span>Show</span> : Add User" href="javascript:void(0);">Add User</a>
        </div>
    </div>
    -->

</div>


<div class="row">
    <div class="form-group col-4">
        <input class="form-control" type="text" id="text_search" name="text_search" placeholder="Search">
    </div>
    <div class="form-group col-2">
        <button id="search" class="btn btn-primary px-3 radius-30">Search</button>
    </div>
    <div class="form-group col-2">
        <button id="reset" class="btn btn-danger px-3 radius-30">Reset</button>
    </div>

</div>

<div class="card">
    <div class="card-header">
        <a id="assign_leads" href="javascript:void(0)"><i class="fa fa-user"></i> Assign Leads</a>
    </div>
    <div class="card-body">

        <div class="table-responsive">
            <table id="userTable" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th></th>
                        <th>#</th>
                        <th>DETAILS</th>
                        <th>DATE</th>
                        <th>AGENT</th>
                        <th>PROJECT</th>
                        <th>STAGE</th>
                        <th>CATEGORY</th>
                        <th>SOURCE</th>
                        <th>REMARK</th>
                        <th>DECISION</th>
                        <th></th>
                    </tr>
                </thead>

            </table>
        </div>
    </div>
</div>

@section scripts
{
    <script>
        //$('#role_id').selectize({
        //    plugins: ['remove_button'],
        //});

       $(document).ready(function () {
           var leads_ids = [];
           var datatable = $('#userTable')
               .DataTable({/* we added this from the other system to have same search and same pagination*/
                   "dom": "<'dt--top-section '<'row'<'col-sm-12 col-md-6 d-flex justify-content-md-start justify-content-center'B><'col-sm-12 col-md-6 d-flex justify-content-md-end justify-content-center mt-md-0 mt-3'f>>>" +
                       "<'table-responsive  'tr>" +
                       "<'dt--bottom-section d-sm-flex justify-content-sm-between text-center'<'dt--pages-count  mb-sm-0 mb-3'i><'dt--pagination'p>>",
                   "buttons": {
                       buttons: [
                           { extend: 'copy', className: 'btn btn-sm' },
                           { extend: 'csv', className: 'btn btn-sm' },
                           { extend: 'excel', className: 'btn btn-sm' },
                           { extend: 'print', className: 'btn btn-sm' }
                       ]
                   },
                   "oLanguage": {
                       "oPaginate": { "sPrevious": 'Perivous', "sNext": 'Next' },
                       "sInfo": "Showing page _PAGE_ of _PAGES_",
                       "sSearch": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-search"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>',
                       "sSearchPlaceholder": "Search...",
                       "sLengthMenu": "Results :  _MENU_",
                   },
                    "processing": true,
                    "serverSide": true,
                    "scrollX": true,
                    "stateSave": false,
                    "lengthChange": false,
                    "dom": 'Bfrtip',

                    "buttons": ['copy', 'excel', 'pdf', 'print'],
                    "pageLength":10,
                    "rowId": 'id',
                    "ajax": {
                        "url": "@Url.Action("Index", "Lead")",
                        "type": "POST",
                        "datatype": "json",
                        "dataSrc": "data"
                    },
                   "columns": [
                       {
                           "data": "id", "id": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               html += "<input type='checkbox' class='selectLead' />";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {

                           "data": "id",
                           "searchable": true
                       },
                       {
                           "data": "first_name", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               html += "<a style='text-decoration:underline' href='javascript:void(0)' class='lead_details'>" + oData.first_name.toUpperCase() + " " + oData.last_name.toUpperCase() + "</a><br>";
                               if (oData.phone1) {
                                   html += "Phone1 : " + oData.phone1;
                                   html += ` <a href='https://api.whatsapp.com/send/?phone=+20 ${oData.phone1}&text=Hello' target='_blank'><i class='fab fa-whatsapp'></i></a>
                                             <a href = 'tel://${oData.phone1}' ><i class="fas fa-phone"></i></a > <br>`;
                               }
                               if (oData.phone2) {
                                   html += "Phone2 : " + oData.phone2;
                                   html += ` <a href='https://api.whatsapp.com/send/?phone=+20 ${oData.phone2}&text=Hello' target='_blank'><i class='fab fa-whatsapp'></i></a>
                                             <a href = 'tel://${oData.phone2}' ><i class="fas fa-phone"></i></a > <br>`;
                               }
                               if (oData.alternative_numbers) {
                                   html += "Alt Number : " + oData.alternative_numbers;
                                   html += ` <a href='https://api.whatsapp.com/send/?phone=+20 ${oData.alternative_numbers}&text=Hello' target='_blank'><i class='fab fa-whatsapp'></i></a>
                                             <a href = 'tel://${oData.alternative_numbers}' ><i class="fas fa-phone"></i></a > <br>`;
                               }
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       }, {
                           "data": "created_at_string", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.created_at_string)
                                   html += oData.created_at_string;
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "assigned_user_id", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.assigned_user_name)
                                   html += oData.assigned_user_name;
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "leadProjects", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.leadProjects) {
                                   console.log(oData.leadProjects)
                                   oData.leadProjects.forEach(function (project) {
                                       html += project.project_name + "<br>";
                                    });
                               }
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "lead_stage_id", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.lead_stage_id)
                               {
                                   if (oData.lead_stage_id == 1)
                                       html += "New";
                                   else if(oData.lead_stage_id == 2)
                                       html += "FollowUp";
                                   else if (oData.lead_stage_id == 3)
                                       html += "Negotiation";
                                   else if (oData.lead_stage_id == 4)
                                       html += "Meeting";
                                   else if (oData.lead_stage_id == 5)
                                       html += "Meeting";
                               }
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "lead_category_id", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.lead_category_name)
                                   html += oData.lead_category_name;
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "source_id", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.source_name)
                                   html += oData.source_name;
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "remark", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.remark)
                                   html += oData.remark;
                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                       {
                           "data": "decision_id", "searchable": true,
                           fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                               var html = "";
                               if (oData.decision_id) {
                                   if (oData.decision_id == 1)
                                       html = "Qualified";
                                   else
                                       html = "Not Qualified";
                               }

                               else
                                   html += "-";
                               $(nTd).html("<span class='action-column'>" + html + "</span>");
                           }
                       },
                        {
                            "data": "id", "searchable": true,
                            fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                                var html = "";
                                var userId = '@Session["id"].ToString()';
                                var isSuperAdmin = '@CRM.Auth.isA.superAdmin()';
                                var isAdmin = '@CRM.Auth.isA.admin()';

                                html += `
                                <div class="btn-group">
                                    <button type="button" class="btn btn-primary dropdown-toggle dropdown-toggle-split" data-toggle="dropdown">
                                        <i class="fa fa-arrow-alt-circle-down"></i>
                                    </button>`;
                                html += '<div class="dropdown-menu">';
                                if ((parseInt(userId) == parseInt(oData.created_by)) || isSuperAdmin == "True" || isAdmin == "True") {
                                    html += `
                                        <a class='dropdown-item' target='_blank' href='/Lead/manageLead/ ${oData.id}' title='Edit'><i class='far fa-edit'></i> Edit</a>
                                        <a class='dropdown-item delete' href='javascript:void(0)' title='Delete'><i class='fas fa-trash'></i>Delete</a>
                                        <a class='dropdown-item assign_agent' href='javascript:void(0)' title='Assign To Sales Agent'><i class='fas fa-user'></i>Assign Agent</a>
                                `;

                                }
                                html += `
                                        <a class='dropdown-item make_activity' href='javascript:void(0)' title='Activity'><i class='fas fa-phone-volume'></i>Make Activity</a>
                                        <a class='dropdown-item make_decision' href='javascript:void(0)' title='Decision'><i class='fas fa-tasks'></i>Take Decision</a>
                                        <a class='dropdown-item change_stage' href='javascript:void(0)' title='Stage'><i class="fas fa-crosshairs"></i>Change Stage</a>`
                                if (oData.lead_stage_id == 5)
                                    html += "<a class='dropdown-item make_deal' href='javascript: void (0)' title='Convert To Deal'><i class='fas fa-check-double'></i>Make Deal</a>";
                                html +=`
                                        <a class='dropdown-item show_interests' href='javascript:void(0)' title='Show Interests'><i class="fas fa-snowflake"></i>Show Interests</a>
                                        <a class='dropdown-item show_activites' href='javascript:void(0)' title='Show Activities'><i class="fas fa-list-ol"></i>Show Activities</a>
                                    </div>
                                </div>`;

                                $(nTd).html("<span class='action-column'>" + html + "</span>");
                            }
                        },
                    ]
                 });

            $('#search').on('click', function () {
                datatable.search($("#text_search").val());
                //datatable.columns(0).search($("#date-form").val());
                //datatable.columns(1).search($("#date-to").val());
                datatable.draw();
            });

            $('#text_search').on('keyup', function (e) {
                if (e.keyCode == 13)
                    $('#search_button').trigger('click');
            });

            $('#reset').on('click', function () {
                $("#text_search").val("");

                $('#search').trigger('click');

            });

            //$('#date-form').bootstrapMaterialDatePicker({
            //    time: false
            //});

            //$('#date-to').bootstrapMaterialDatePicker({
            //    time: false
            //});
           //$("#image").click(function (event) {
           //    var output = document.getElementById('image');
           //    output.src = URL.createObjectURL(event.target.files[0]);
           //});


           $("#assign_leads").click(function () {
               if (leads_ids.length != 0) {
                   var modal = $('#manage_assign_Lead_modal').clone();
                   var action = '@Url.Action("assignLeads", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Assign Leads');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                           leads_ids = [];
                   }, function (response) {

                   }, function (dialog) {
                       dialog.find("#lead_ids").val(leads_ids.join());
                   });
               }
               else {
                   Swal.fire(
                       'Error!',
                       'No Selected Leads!',
                       'error'
                   )
               }
           });

           $(document).on("click", ".assign_agent", function () {

               var data = datatable.row($(this).closest('tr')).data();
                var modal = $('#manage_assign_Lead_modal').clone();
                var action = '@Url.Action("assignLeads", "Lead")';
                modal.find('form').attr('action', action);
                modal.find('#modal_title').text('Assign Leads');
                modal.execModal({
                    progressBar: 'progress_bar',
                    progressText: 'progress_text',
                }, function (response) {
                        datatable.draw();
                }, function (response) {

                }, function (dialog) {
                    dialog.find("#assigned_user_id").val(data.assigned_user_id);

                });

            });

           $(document).on("click", "#add_quick_lead", function () {
               var modal = $('#manage_quick_lead_modal').clone();
                   var action = '@Url.Action("saveQuickLead", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Add Quick Lead');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                   }, function (response) {


                   }, function (dialog) {
                           var property_ids = dialog.find('#property_ids').selectize({
                               plugins: ['remove_button'],
                           });
                   });
           });

           $(document).on("click", ".make_activity", function () {
               var data = datatable.row($(this).closest('tr')).data();
               var modal = $('#manage_make_activity_modal').clone();
                   var action = '@Url.Action("makeActivity", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Make Activity');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                   }, function (response) {


                   }, function (dialog) {
                           dialog.find("#lead_id").val(data.id);
                   });
           });

           $(document).on("click", ".change_stage", function () {
               var data = datatable.row($(this).closest('tr')).data();
               var modal = $('#manage_lead_stage_modal').clone();
                   var action = '@Url.Action("changeStage", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Change Stage');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                   }, function (response) {


                   }, function (dialog) {
                           dialog.find("#id").val(data.id);
                           dialog.find('#lead_stage_id option[value=' + data.lead_stage_id + ']').attr('selected', 'selected');
                   });
           });

           $(document).on("click", ".make_decision", function () {
               var data = datatable.row($(this).closest('tr')).data();
               var modal = $('#manage_lead_decision_modal').clone();
                   var action = '@Url.Action("makeDecision", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Make Decision');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                   }, function (response) {


                   }, function (dialog) {
                           dialog.find("#id").val(data.id);
                           dialog.find('#decision_id option[value=' + data.decision_id + ']').attr('selected', 'selected');
                   });
           });

           $(document).on("click", ".make_deal", function () {
               var data = datatable.row($(this).closest('tr')).data();
               var modal = $('#manage_lead_deal_modal').clone();
                   var action = '@Url.Action("makeDeal", "Lead")';
                   modal.find('form').attr('action', action);
                   modal.find('#modal_title').text('Convert To Deal');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                           datatable.draw();
                   }, function (response) {


                   }, function (dialog) {
                           dialog.find("#id").val(data.id);
                           //dialog.find('#deal_property_id option[value=' + data.deal_property_id + ']').attr('selected', 'selected');
                           dialog.find("#deal_property_price").val(data.deal_property_price);
                           dialog.find('#deal_make_user_id option[value=' + data.deal_make_user_id + ']').attr('selected', 'selected');
                   });
           });

           $(document).on("click", ".lead_details", function () {
               var data = datatable.row($(this).closest('tr')).data();
     
               $("#lead_details_first_name").text(data.first_name);
               $("#lead_details_last_name").text(data.last_name);
               $("#lead_details_email").text(data.email);
               $("#lead_details_primary_phone").text(data.phone1);
               $("#lead_details_secondary_phone").text(data.phone2);
               $("#lead_details_alternative_number").text(data.alternative_numbers);

               var date_of_birth = '-';
               if (data.date_of_birth != null) {
                   var dateValue = new Date
                       (
                           parseInt(data.date_of_birth.replace(/(^.*\()|([+-].*$)/g, ''))
                       );
                   var date_date_of_birth = dateValue.getFullYear() +
                       "-" +
                       ("0" + (dateValue.getMonth() + 1)).slice(-2) +
                       "-" +
                       ("0" + dateValue.getDate()).slice(-2)
                       ;
                   date_of_birth = date_date_of_birth;
               }
               $("#lead_details_date_of_birth").text(date_of_birth);

               var date_of_anniversary = '-';
               if (data.date_of_anniversary != null) {
                   var dateValue = new Date
                       (
                           parseInt(data.date_of_anniversary.replace(/(^.*\()|([+-].*$)/g, ''))
                       );
                   var date_date_of_anniversary = dateValue.getFullYear() +
                       "-" +
                       ("0" + (dateValue.getMonth() + 1)).slice(-2) +
                       "-" +
                       ("0" + dateValue.getDate()).slice(-2)
                       ;
                   date_of_anniversary = date_date_of_anniversary;
               }
               $("#lead_details_date_of_anniversary").text(date_of_anniversary);
               $("#lead_details_source").text(data.source_name);
               $("#lead_details_sales_agent").text(data.sales_agent);
               $("#lead_details_address").text(data.address);
               $("#lead_details_country").text(data.country);

               $("#lead_details_property_type").text(data.property_type_name);
               $("#lead_details_sub_type").text(data.property_type_name);
               $("#lead_details_unit_type").text(data.property_type_name);
               $("#lead_details_requirement").text(data.requirement_name);
               $("#lead_details_budget_min").text(data.budget_min);
               $("#lead_details_budget_max").text(data.budget_max);
               $("#lead_details_min_area_in").text(data.minimum_area);
               $("#lead_details_max_area_in").text(data.maximum_area);
               $("#lead_details_area_metric").text(data.area_metric);
               $("#lead_details_remark").text(data.remark);

               $("#manage_lead_details_modal").modal('show');
           });

           $(document).on("click", ".show_activites", function () {
               var data = datatable.row($(this).closest('tr')).data();
               var modal = $('#manage_show_lead_activites_modal').clone();
                   modal.find('#modal_title').text('Activities');
                   modal.execModal({
                       progressBar: 'progress_bar',
                       progressText: 'progress_text',
                   }, function (response) {
                   }, function (response) {


                   }, function (dialog) {
                           if (data.leadActivities) {
                               var html = '';
                               data.leadActivities.forEach(function (value) {
                                   var id = "-";
                                   var activity_name = "-";
                                   var activity_duration = "-";
                                   var note = "-";
                                   var activity_date_time = "-";

                                   if (value.id)
                                       id = value.id;
                                   if (value.activity_name)
                                       activity_name = value.activity_name;
                                   if (value.activity_duration)
                                       activity_duration = value.activity_duration;
                                   if (value.note)
                                       note = value.note;

                                   if (value.activity_date_time != null) {
                                       var dateValue = new Date
                                           (
                                               parseInt(value.activity_date_time.replace(/(^.*\()|([+-].*$)/g, ''))
                                           );
                                       var date_activity_date_time = dateValue.getFullYear() +
                                           "-" +
                                           ("0" + (dateValue.getMonth() + 1)).slice(-2) +
                                           "-" +
                                           ("0" + dateValue.getDate()).slice(-2)
                                           ;
                                       activity_date_time = date_activity_date_time;
                                   }
                                   html += `<tr>
                                   <td>${id}</td>
                                   <td>${activity_name}</td>
                                   <td>${activity_duration}</td>
                                   <td>${note}</td>
                                   <td>${activity_date_time}</td>
                                   </tr>`;
                               });
                           }
                           dialog.find("#show_activities_table").append(html);
                   });
           });

           $(document).on("click", ".show_interests", function () {
               var data = datatable.row($(this).closest('tr')).data();
               $("#projects_table_body").remove();
               $("#properties_table_body").remove();
               $("#developers_table_body").remove();
               if (data.leadProjects) {
                   var leadProjectssHtml = '';
                   leadProjectssHtml += "<tbody id='projects_table_body'>";
                   data.leadProjects.forEach(function(value){
                       leadProjectssHtml += `<tr>
                       <td>${value.id}</td>
                       <td>${value.project_name}</td>
                       </tr>`;
                   });
                   leadProjectssHtml += "</tbody>"
                   $("#projects_table").append(leadProjectssHtml);
               }

               if (data.leadProperties) {
                   var leadPropertiesHtml = '';
                   leadPropertiesHtml += "<tbody id='properties_table_body'>";
                   data.leadProperties.forEach(function(value){
                       leadPropertiesHtml += `<tr>
                       <td>${value.id}</td>
                       <td>${value.property_name}</td>
                       </tr>`;
                   });
                   leadProjectssHtml += "</tbody>";
                   $("#properties_table").append(leadPropertiesHtml);
               }

               if (data.leadDevelopers) {
                   var leadDevelopersHtml = '';
                   leadDevelopersHtml += "<tbody id='developers_table_body'>";
                   data.leadDevelopers.forEach(function(value){
                       leadDevelopersHtml += `<tr>
                       <td>${value.id}</td>
                       <td>${value.developer_name}</td>
                       </tr>`;
                   });
                   leadDevelopersHtml += "</tbody>";
                   $("#developers_table").append(leadDevelopersHtml);
               }



               $("#manage_show_interests_modal").modal('show');
           });

           $(document).on("click", ".selectLead", function () {
               var data = datatable.row($(this).closest('tr')).data();
               if ($(this).is(':checked')) {
                   leads_ids.push(data.id);

               }
               else {
                   const index = leads_ids.indexOf(data.id);
                   if (index > -1) {
                       leads_ids.splice(index, 1);
                   }
               }
               console.log(leads_ids);

            });

            $(document).on("click", ".delete", function () {
                var data = datatable.row($(this).closest('tr')).data();
                warningBox("Are you sure you want to delete this Lead (<b>" + data.name +"</b>) ?", function () {
                    $.ajax({
                        type: "GET",
                        url: '@Url.Action("deleteLead", "Lead")',
                        contentType: "application/json; charset=utf-8",
                        data: { "id": data.id },
                        datatype: "json",
                        success: function (data) {
                            datatable.draw();
                        },
                        error: function () {
                            alert("Dynamic content load failed.");
                        }
                    });
                });
            });

        });

    </script>
}

